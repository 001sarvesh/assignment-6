//  Write a program to calculate sum of first N even natural numbers
#include <stdio.h>
int main()
{
    int i,n;
    int sum = 0;
    for ( i = 0; i <= n; i++)
    {
        sum = sum + 2*i+2;
    }
    printf("%d\n", sum);
    return 0;
}